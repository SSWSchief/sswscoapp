"use client";import * as React from "react";import {Topbar} from "@/components/dispatcher/Topbar";import {Button} from "@/components/ui/Button";import {Card} from "@/components/ui/Card";import {Badge} from "@/components/ui/StatusBadge";import {Table,TBody,TD,TH,THead,TR} from "@/components/ui/Table";import {useExpandedOperations} from "@/components/system/ExpandedOperationsProvider";import {useOperations} from "@/components/system/OperationsProvider";import {useToast} from "@/components/system/ToastProvider";import {InvoiceModal} from "@/components/dispatcher/InvoiceModal";import {downloadCsv} from "@/lib/client-download";import {formatDate} from "@/lib/utils";import type {InvoiceRecord} from "@/lib/types";
export default function InvoicesPage(){const {invoices}=useExpandedOperations();const {customers,canMutate}=useOperations();const {toast}=useToast();const [open,setOpen]=React.useState(false);const [editing,setEditing]=React.useState<InvoiceRecord>();const [exporting,setExporting]=React.useState(false);const receivables=invoices.filter(i=>!["paid","closed","void"].includes(i.status)).reduce((n,i)=>n+i.amountCents,0);const exportInvoices=async()=>{setExporting(true);try{await downloadCsv("/api/exports/invoices","invoices.csv");toast("Invoice CSV downloaded.",{tone:"success"});}catch(error){toast(error instanceof Error?error.message:"Invoice export could not be downloaded.",{tone:"error"});}finally{setExporting(false);}};return <><Topbar title="Invoices" action={<Button onClick={()=>{setEditing(undefined);setOpen(true)}}>New Invoice</Button>}/><div className="flex-1 overflow-y-auto p-4 sm:p-6 space-y-5"><div className="grid grid-cols-2 gap-4"><Metric label="Receivables" value={new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(receivables/100)}/><Metric label="Open Invoices" value={invoices.filter(i=>!["closed","void"].includes(i.status)).length}/></div><Card><div className="flex justify-end border-b border-brand-ice p-4"><Button variant="secondary" disabled={!canMutate||exporting} onClick={()=>void exportInvoices()}>{exporting?"Exporting…":"Export CSV"}</Button></div><Table><THead><TH>Invoice</TH><TH>Customer</TH><TH>Amount</TH><TH>Status</TH><TH>Due</TH><TH/></THead><TBody>{invoices.map(i=><TR key={i.id}><TD className="font-semibold">{i.invoiceNumber}</TD><TD>{customers.find(c=>c.id===i.customerId)?.name??"—"}</TD><TD>{new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(i.amountCents/100)}</TD><TD><Badge tone={i.status==="overdue"?"amber":["paid","closed"].includes(i.status)?"green":"blue"} label={i.status}/></TD><TD>{formatDate(i.dueDate)}</TD><TD><button className="min-h-11 text-brand-blue" onClick={()=>{setEditing(i);setOpen(true)}}>Edit</button></TD></TR>)}</TBody></Table>{!invoices.length&&<p className="p-8 text-center text-brand-steel">No invoices yet.</p>}</Card></div><InvoiceModal open={open} onClose={()=>setOpen(false)} invoice={editing}/></>}
function Metric({label,value}:{label:string;value:React.ReactNode}){return <Card className="p-5"><div className="font-heading text-3xl font-bold">{value}</div><div className="text-sm uppercase text-brand-steel">{label}</div></Card>}
